# IIT-assignment-4

TO RUN THIS PROGRAM
- download and extract the program if needed
- if the extracter creates an extra file holding the original folder with the code then remove it (essentially move the IIT assignment 4 file with programs into the download outside of ANY other file to run)
- run "python -m src.Main" through VScode terminal or open powershell change directory to inside the folder containing all the code using "cd" func
